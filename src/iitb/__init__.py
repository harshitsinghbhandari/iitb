"""iitb: a command-line surface for IIT Bombay portals, built agent-first.

This package is the shell and nothing more: the command tree, the help text,
the output envelope, and the error-code registry. Every implementation lives
in the private `iitb_core` package, which this one imports and never
inspects.
"""

__version__ = "0.1.0"
